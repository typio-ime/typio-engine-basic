# Explanation

Understanding-oriented documentation. These pages explain why `typio-engine-basic` works the way it does.

| Page | Topic |
|------|-------|
| [Compose state machine](compose-state-machine.md) | How the two-key compose logic handles input, pre-edit, commit, and cancel. |
| [Why `cdylib`](why-cdylib.md) | Why the crate is compiled as a dynamic library rather than a static library or executable. |
| [typio-abi integration](typio-abi-integration.md) | Why the engine imports ABI types from a shared crate instead of replicating them by hand. |
